# 关于动画
先动画再跳转就是为了动画而动画，但这样用户体验会非常差，因为他被强制看动画。
应该是页面资源加载时给用户看动画
以vue为例
创建一个createLoadingPage高阶函数
函数调用时返回两个函数openAnimation和closeAnimation
在页面的beforeRouteEnter中设置开启动画
mounted中或某请求加载完就关闭动画
如果加载比较块一闪而过反而影响体验
这时候可以改进上面函数，传递一个最小动画时间比如300ms
